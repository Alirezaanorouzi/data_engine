services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dataengine
      POSTGRES_PASSWORD: dataengine
      POSTGRES_DB: dataengine
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dataengine -d dataengine"]
      interval: 5s
      timeout: 5s
      retries: 10

  parse-worker:
    build: ./worker
    restart: unless-stopped
    ports:
      - "8090:8090"
    env_file:
      - .env
    environment:
      PORT: "8090"
      OCR_ENABLED: "true"
      OCR_MODEL: "qwen/qwen3.7-flash"
      OCR_MIN_TEXT_CHARS: "30"
      OCR_RENDER_SCALE: "2.0"
    volumes:
      - ./storage:/storage:ro

volumes:
  pgdata:
